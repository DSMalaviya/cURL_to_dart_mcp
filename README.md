# curl-to-dart-mcp

MCP server that converts cURL JSON responses into Dart models using QuickType.

## Usage (Antigravity / Cursor)

```json
{
  "name": "curl-dart",
  "command": "npx",
  "args": ["curl-to-dart-mcp"]
}